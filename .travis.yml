language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -f bobcat1111/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bobcatt11/docker-react npm run test
